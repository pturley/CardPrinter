%h1
  Story Wall
%hr
.story-wall
  %span.links
    Card Printer
    - @cards.each do |card|
      #printable.Card
        .number
          Number:
          = card.number
        .name
          Title:
          = card.title
        .estimate
          Estimate:
          = card.estimate
